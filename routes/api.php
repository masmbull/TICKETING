<?php

/*
|--------------------------------------------------------------------------
| API Routes — intentionally empty
|--------------------------------------------------------------------------
| This file is NOT registered in bootstrap/app.php (withRouting has no
| `api:` entry), so nothing here was ever reachable. All JSON endpoints used
| by the frontend (/api/search, /api/subcategories, /api/category-priority)
| live in routes/web.php behind session auth + CSRF.
|
| If real stateless API endpoints are needed later, register this file and
| install/configure Laravel Sanctum first — do not reference the `sanctum`
| guard while it is unavailable.
*/
